# Copy to .env.local. ONLY public values belong here — the web tier holds no
# privileged secrets by design (plan §4): anon key + URLs, nothing else.
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
NEXT_PUBLIC_API_BASE_URL=https://api.nashr.uz
