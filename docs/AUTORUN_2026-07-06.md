# AUTORUN 2026-07-06 — unattended run journal + end-of-run report

Auto-mode constrained run (2h). Work order: Vertex WIP → 5B-Q5b caching → migration 005
(write only) → P1 preflight script (write only) → FastAPI auth + identity → Next.js
skeleton. Hard rules honored: no infra provisioned, no migration applied, no live
Vertex/Gemini calls, no gate marked passed, no merge, Codex review before every commit
(no self-clear). git log is truth; nothing is claimed below that the log doesn't show.

## STATUS BOARD (updated as the run progresses)

| # | Item | State | Commit | Codex |
|---|------|-------|--------|-------|
| 1 | Vertex transport (LLM_TRANSPORT) | COMMITTED | `6ab511a` | R1: 2 HIGH + 2 MEDIUM found→fixed; R2: 1 RISK found→fixed; R3: CLEAN |
| 2 | 5B-Q5b Gemini brain context cache | COMMITTED (2026-07-07) | `e87c072` | R1+R2 (4+2 findings fixed) + 5-lens panel: 6 findings, all fixed+test-pinned |
| 3 | Migration 005_identity (SQL only) | PARKED uncommitted (never applied) | — | pending quota |
| 4 | Preflight script (not run) | PARKED uncommitted (never run) | — | pending quota |
| 5 | FastAPI auth + identity | PARKED uncommitted | — | pending quota |
| 6 | Next.js skeleton | PARKED uncommitted | — | pending quota |

(Authoritative per-item detail + the FINAL STATUS BOARD are below.)

## CODEX OUTAGE (21:01) — parking protocol in effect

`codex exec` returned "usage limit … try again at 11:48 PM" mid-run (item 2, round 3).
Reset is outside the run window ⇒ per constraint 3, item 2 and every later item are
BUILT + TESTED but PARKED UNCOMMITTED. Item boundaries are the per-item file lists
below — stage exactly that list to commit one item.

## ITEM 1 — Vertex transport — COMMITTED `6ab511a`

- WHAT: finished the in-flight WIP: `LLM_TRANSPORT` (default vertex) → AsyncAnthropicVertex
  (VERTEX_PROJECT, region default `global`); direct API as fallback; transport frozen at
  construction, SDK client built LAZILY on first call (fixes 18 drafter-suite failures the
  WIP itself caused — bare `LLMClient()` no longer crashes without Vertex env);
  `vertex_model_id()` ALLOWLIST translation (`claude-haiku-4-5-20251001` →
  `claude-haiku-4-5@20251001` per the official Vertex model table, alias `claude-sonnet-4-6`
  passes through); non-global-region cost-premium warning; probe padded past the 1,024-token
  cache floor (was ~60 tokens = guaranteed false-FAIL); google-auth pinned explicitly.
- VERIFIED: full suite 1548/32 at commit; ruff+format clean; pyright 0/0/0; Vertex model-id
  table verified against live Anthropic docs (fetched 2026-07-06).
- NOT VERIFIED (needs the P0 GCE VM): any live Vertex call, cache-read billing, GCP billing
  visibility, the probe itself. ACCEPTED-WITH-ARGUMENT (Codex HIGH-2): vertex-by-default
  breaks an ANTHROPIC_API_KEY-only env unless LLM_TRANSPORT=anthropic — plan-of-record
  choice; no live deployment exists today.

## ITEM 2 — 5B-Q5b brain context cache — COMMITTED `e87c072` (2026-07-07)

PANEL UPDATE (2026-07-07): the 5-lens Codex panel ran and returned 6 findings, all
fixed + test-pinned before commit: in-lock re-check of the create-failure disable
(3 lenses converged), 30s bound on caches.create (was unbounded under the session
lock), TimeoutError added to the cached-call fallback, no handle fetched for
non-default tool configs (mislabel), cache name logged as suffix only, and the
stale BUILD_STATE 'NO wiring' claim superseded by a dated entry. Suite 1593/32 at
commit. Original parked-state notes below kept for the record.

- FILES (stage exactly these to commit item 2):
  `packages/core/gemini_cache.py` (NEW) · `packages/core/gemini.py` ·
  `packages/core/brain_loop.py` · `packages/bot/sessions/driver.py` · `conftest.py` ·
  `tests/unit/test_gemini_cache.py` (NEW) · `tests/unit/test_gemini_client.py` ·
  `tests/unit/test_brain_loop.py` · `tests/unit/test_brain_driver.py` · `.env.example` ·
  `docker-compose.yml`
- WHAT: `BrainContextCache` (PROCESS-WIDE singleton via driver.py `_shared_brain_context_cache`
  — a per-driver cache would be created+consumed once per message, worse than uncached;
  Codex R2 finding), explicit cache over `assemble_brain_system()`+`build_edit_slides_tool()`,
  TTL 3600s with 120s client-side margin, create-failure disables for process lifetime,
  pinned-system mismatch bypasses. `generate_with_tools(cached_content=)` omits
  system/tools/tool_config on cached calls (API mutual exclusion); non-default tool config
  (Way 1 forced-ANY) bypasses with a warning. `run_brain_loop(context_cache=)` fetches a
  handle per iteration; a cached-call APIError invalidates + retries uncached WITHOUT
  consuming an iteration (attempts -= 1, non-looping by construction). Env gate
  `GEMINI_BRAIN_CACHE` default OFF in code, ON in compose (the P0/VM gate observes it).
  Root conftest autouse fixture keeps the suite hermetic against a dev shell exporting it.
  Cross-event-loop lock rebinding (`_loop_lock`) for successive asyncio.run in gate scripts.
- VERIFIED: full suite 1566 passed / 32 skipped; ruff + format clean on changed files;
  pyright 0/0/0. Codex R1+R2 findings all fixed and test-pinned.
- NOT VERIFIED: Codex round-3 confirmation of the R2 fixes (quota) — **run before commit**:
  stage the file list above, then
  `codex exec --sandbox read-only "review the staged diff …"` (prompt: confirm the
  while-loop attempts accounting across all paths + the loop-lock rebinding). Also not
  verified: any LIVE cached call (needs VM + GEMINI_BRAIN_CACHE=1) — cache read>0 in
  `gemini_call_complete` logs is a P0 gate item. COST NOTE: `gemini_cost_for` deliberately
  bills cached tokens at full input rate (conservative ceiling); modeling the discount
  needs confirmed Gemini 3.x cache rates from GCP billing.

## ITEM 3 — migration 005_identity — WRITTEN, NOT APPLIED, PARKED UNCOMMITTED

- FILES: `supabase/migrations/005_identity.sql` (always applied) +
  `supabase/migrations/005b_identity_path_b.sql` (apply ONLY if preflight exits 2).
- WHAT: `user_auth_identities(provider ∈ {telegram,email}, external_id, user_id FK→users
  cascade, auth_user_id FK→auth.users set-null, PK(provider,external_id))`; lowercase-email
  + digits-telegram CHECKs; unique partial index on auth_user_id (one auth user → at most
  one app identity — what makes Path B's app_uid() unambiguous); RLS owner-SELECT only
  (writes service-role — linking/merge are API operations). 005b: `app_uid()` SECURITY
  DEFINER bridge + mechanical rewrite of ALL 20 owner policies from 001/002/004/005
  (`auth.uid()::uuid` → `app_uid()`), bodies otherwise verbatim from the originals
  (extracted from the migration files, not from memory).
- NOT VERIFIED: not applied anywhere (prohibited), not parsed by a live Postgres, no Codex
  review yet (quota). HUMAN: apply 005 to Supabase AFTER the preflight decides the path;
  apply 005b ONLY on Path B.

## ITEM 4 — P1 preflight script — WRITTEN, NOT RUN, PARKED UNCOMMITTED

- FILE: `scripts/preflight_identity_path.py`. Checks BOTH surfaces per amendment 4:
  (1) minted HS256 JWT (stdlib mint, self-checked offline) reads its own project row
  through RLS'd PostgREST; (2) a Realtime postgres_changes subscription authorized with
  the SAME token receives an UPDATE event (service key triggers it). Exit 0=Path A,
  2=Path B (naming WHICH check failed + likeliest cause: bad signature vs RLS mismatch vs
  missing `supabase_realtime` publication — the last one means FIX PUBLICATION AND RE-RUN,
  not Path B), 3=config error. Creates + deletes a throwaway user/project unless
  PREFLIGHT_TEST_USER_ID is provided.
- RUN COMMAND (human): env needs SUPABASE_URL, SUPABASE_SERVICE_KEY, SUPABASE_ANON_KEY,
  SUPABASE_JWT_SECRET → `python scripts/preflight_identity_path.py`.
- VERIFIED: ruff/format/pyright clean; parses; JWT mint self-check (header/claims/
  signature) green. NOT VERIFIED: never run against a live project (prohibited); the
  supabase-py Realtime call path is code-read only.

## ITEM 5 — FastAPI auth + identity (§5) — BUILT + GREEN, PARKED UNCOMMITTED

- FILES: `packages/api/{app.py,run.py}` · `packages/api/middleware/auth.py` ·
  `packages/api/routes/auth.py` · `packages/api/services/{telegram_auth.py,tokens.py,
  identity.py}` · `packages/platform/identity_store.py` (NEW) ·
  `packages/platform/config.py` (adds supabase_anon_key/supabase_jwt_secret/
  app_jwt_ttl_seconds/web_cors_origins) · `packages/core/models/identity.py` (NEW) ·
  `packages/core/enums.py` (IdentityProvider) · `requirements.txt` (fastapi/uvicorn pins)
  · `tests/unit/{test_identity_service.py,test_api_auth_routes.py}` (NEW) · migration 005
  REVISED (surrogate id PK per core-models rule; users.telegram_id DROP NOT NULL —
  email-first users were structurally impossible; merge_users() SECURITY DEFINER RPC —
  PostgREST cannot span a transaction client-side).
- WHAT: Telegram door (initData HMAC constant-time + 5-min freshness, reason-coded
  errors), email door (GoTrue /auth/v1/user verification → app-JWT exchange; signing-
  config agnostic), link+merge (both-doors-proven → atomic RPC), Path A mint/verify
  (stdlib HS256, fail-closed on missing secret → 503), bearer dependency, CORS allowlist
  from env, health. Vercel tier holds zero privileged secrets by construction.
- VERIFIED: 23 new tests (crypto vectors real; Supabase/Telegram boundaries faked); full
  suite 1589 passed / 32 skipped; ruff + format clean; pyright strict 0/0/0.
- NOT VERIFIED: no live Supabase round-trip (identity_store code-read + fake-tested only);
  no Codex review yet (quota); the P1 gate (two-account RLS negative test, real Mini App
  login) needs the deployed stack + applied migration.

## HUMAN ACTIONS NEEDED (accumulating; final list at end of run)

1. After 23:48: run the parked Codex reviews per item (file lists above), then commit each
   item separately.
2. P0 first step (not done here, prohibited): provision GCE VM e2-small in nashr-prod,
   Docker+compose+Caddy, deploy, run `scripts/probe_vertex_claude_llm.py` in the container,
   run one real generation, record $/deck + VM burn rate in BUILD_STATE.
3. Item 2 live gate rides the same VM: `GEMINI_BRAIN_CACHE=1` (compose default), grep
   `gemini_call_complete` for `cached_input_tokens > 0` on a Way 2 turn.

## ITEM 6 — Next.js skeleton (packages/web) — BUILT + GREEN, PARKED UNCOMMITTED

- FILES: `packages/web/{package.json,tsconfig.json,next.config.mjs,.env.local.example,
  .gitignore}` · `app/{layout.tsx,page.tsx,login/page.tsx,auth/callback/page.tsx,
  projects/page.tsx}` · `lib/{api.ts,session.ts,supabase.ts,telegram.ts}` — replaces the
  one-line stub `packages/web/index.ts` (deleted).
- WHAT: both login doors (Mini App auto-login off `window.Telegram.WebApp.initData`;
  magic-link → /auth/callback → app-JWT exchange, Supabase session signed out after);
  authed /projects page reading the user's own rows through RLS with the app JWT set on
  BOTH PostgREST headers and `realtime.setAuth` (mirrors the preflight's two surfaces);
  zero privileged env (`.env.local.example` carries only the anon key + URLs). No `any`
  anywhere; typed Telegram bridge.
- VERIFIED: `npm install` (34 packages), `next build` green (5 routes prerendered),
  `tsc --noEmit` clean.
- NOT VERIFIED: never run against live Supabase/API (prohibited); no Codex review
  (quota); Vercel deploy is a human step.

## FINAL STATUS BOARD

| # | Item | State | Commit | Codex |
|---|------|-------|--------|-------|
| 1 | Vertex transport | COMMITTED | `6ab511a` | 3 rounds, final CLEAN |
| 2 | 5B-Q5b brain cache | COMMITTED | `e87c072` | R1+R2 + 5-lens panel (6 findings fixed+pinned) |
| 3 | Migration 005 + 005b | PARKED uncommitted (never applied) | — | pending quota |
| 4 | Preflight script | PARKED uncommitted (never run) | — | pending quota |
| 5 | FastAPI auth + identity | PARKED uncommitted | — | pending quota |
| 6 | Next.js skeleton | PARKED uncommitted | — | pending quota |

Suite at end of run: **1589 passed / 32 skipped**; pyright strict `packages/` 0/0/0;
ruff + format clean on all touched files (the 3 documented payment_flow SIM105s remain
the pre-existing baseline); `next build` + `tsc --noEmit` green. git log shows exactly
ONE new commit (`6ab511a`) — nothing else is claimed as committed.

FORKS PARKED: none — every design decision either followed plan §5/§6 verbatim or was
an accepted-with-argument recorded inline (item 1 vertex-default; item 2 conservative
cached-token pricing; item 5 schema corrections, which FIX plan-vs-schema conflicts
rather than fork them: surrogate id, nullable telegram_id, merge RPC).

## EXACT HUMAN ACTIONS, IN ORDER

1. **After 23:48 — Codex reviews + commits** (one item at a time, stage ONLY that item's
   file list from its section above):
   - Item 2: prompt Codex to confirm the R2 fixes (while-loop attempts accounting across
     all paths; loop-lock rebinding). Then commit.
   - Items 3+4 (can be one review): migration SQL correctness (005/005b) + preflight
     script logic. Then commit (do NOT apply/run them).
   - Item 5: auth surface review (initData HMAC, token mint/verify, merge flow, fail-
     closed paths, CORS). Then commit.
   - Item 6: web skeleton review. Then commit. NOTE: `scripts/_probe_deck_numbers.py`
     (modified) and `scripts/_forensic_9330c308.py` (untracked) are PRE-RUN forensic
     scaffolding, deliberately left out of every item; keep or drop at your call.
2. **P0 (needs you; prohibited for the autorun):** provision the GCE VM (e2-small,
   nashr-prod, trial credits): Docker + compose + Caddy + repo clone + `.env` (real
   values incl. `LLM_TRANSPORT=vertex`, `VERTEX_PROJECT=nashr-prod`, vertex-key.json) →
   `docker compose up -d --build` → `docker exec -e PYTHONPATH=/app -w /app nashr-bot
   python scripts/probe_vertex_claude_llm.py` → one real generation → record **$/deck
   AND VM monthly burn** in BUILD_STATE (P5-gate credit projection needs both).
3. **Item 2 live gate rides the VM:** compose defaults `GEMINI_BRAIN_CACHE=1`; grep
   `gemini_call_complete` for `cached_input_tokens > 0` on a Way 2 turn.
4. **P1 sequence:** run `python scripts/preflight_identity_path.py` (env per its
   docstring; needs SUPABASE_ANON_KEY + SUPABASE_JWT_SECRET from the dashboard) → apply
   `005_identity.sql` (+ `005b` ONLY on exit 2) in the SQL editor → deploy the `api`
   compose service (`python -m packages.api.run`, port 8090, Caddy route) → point the
   web app's `.env.local` at it → Vercel deploy → P1 gate (two-account RLS negative
   test; manual approval mode continues through P1 per your instruction).
5. Add `SUPABASE_ANON_KEY` / `SUPABASE_JWT_SECRET` / `WEB_CORS_ORIGINS` / `API_PORT` to
   the VM `.env` (config.py reads them; `.env.example` documents the first two only —
   extend it when convenient; the autorun avoided further edits there mid-item-2).

(Sections for items 3-6 appended below as they are built.)
